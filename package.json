{
  "name": "mnpdf",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Minimal local-only PDF reader / highlighter",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "tauri": "tauri",
    "icon": "node scripts/gen-icon.mjs && tauri icon dev-assets/icon-src.png",
    "samples": "node scripts/make-samples.mjs",
    "postinstall": "node scripts/copy-static.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.9.0",
    "@tauri-apps/plugin-dialog": "^2.4.0",
    "pdf-lib": "^1.17.1",
    "pdfjs-dist": "^4.10.38"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.0",
    "vite": "^5.4.20"
  }
}
